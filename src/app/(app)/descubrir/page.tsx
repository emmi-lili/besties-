import { PhasePlaceholder } from "@/components/shared/phase-placeholder"

export default function DescubrirPage() {
  return (
    <PhasePlaceholder
      title="Descubrir"
      phase="Fase 4"
      detail="Feed con score, razón y swipe. El seed de 12 perfiles ya está en el mock."
    />
  )
}
